{
  "scripts": {
    "delete": "aws stepfunctions --endpoint-url http://localhost:8083 delete-state-machine --state-machine-arn 'arn:aws:states:us-east-1:101010101010:stateMachine:DemoStateMachine' &",
    "start": " serverless offline start --stage local-dev"
  },
  "devDependencies": {
    "serverless-offline": "^12.0.4",
    "serverless-offline-lambda": "^1.0.6",
    "serverless-step-functions": "^3.14.0",
    "serverless-step-functions-local": "^0.5.0"
  },
  "dependencies": {
    "@aws-sdk/client-sfn": "^3.414.0",
    "aws-sdk": "^2.1459.0"
  }
}
